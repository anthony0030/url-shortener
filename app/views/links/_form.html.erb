<%= form_with model: link, data: { controller: "reset-form", action: "turbo:submit-end->reset-form#reset" } do |form| %>
  <div class="form-group flex gap-2 items-center">
    <%#= form.label :title, class: "form-label" %>
    <%= form.url_field :url, required: true, autofocus: true, placeholder: "https://example.org", class: "form-control flex-grow rounded" %>
    <%= form.button "Save", class: "btn btn-success h-full" %>
  </div>
<% end %>
