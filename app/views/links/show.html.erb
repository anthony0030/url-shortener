<%= link_to "← Back to all links", root_path, class: "btn btn-light mb-4" %>
<h1><%= @link.url %></h1>

<%= link_to "Edit", edit_link_path(@link) %>
<%= line_chart @link.views.group_by_day(:created_at, range: 1.weeks.ago..Time.now, expand_range: true).count %>
