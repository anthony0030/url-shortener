<%= render partial: "form", locals: { link: @link } %>

<div id="links" class="flex flex-col divide-y divide-gray-100 mb-5">
  <%= render @links %>
</div>

<% if @pagy.pages > 1 %>
  <div class="flex justify-center mb-5">
    <%== pagy_nav(@pagy) %>
  </div>
<% end %>
